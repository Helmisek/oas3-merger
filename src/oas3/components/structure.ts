export enum OAS3Component {
  schemas = 'schemas',
  parameters = 'parameters',
  securitySchemes = 'securitySchemes',
  requestBodies = 'requestBodies',
  responses = 'responses',
  headers = 'headers',
  examples = 'examples',
  links = 'links',
  callbacks = 'callbacks',
}
